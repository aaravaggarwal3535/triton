# Installing Triton
## via torch
```
with cuda proper command can be get from : [text](https://pytorch.org/get-started/locally/)
pip install torch
```

## via direct install
```
pip install triton
```