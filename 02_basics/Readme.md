# Triton

## Introduction:
```
Triton is an open-source GPU programming language developed by researchers at OpenAI. 
Created because CUDA is verbose
Triton break program → block of element
Unlike cuda
Thread → wrap → block → grid
Either install through torch or default by : pip install triton
```